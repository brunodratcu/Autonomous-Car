{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "startCommand": "sh -c 'ls -la; if [ -f main.py ]; then M=main; else M=app; fi; echo \"[boot] usando $M:app\"; exec gunicorn $M:app --bind 0.0.0.0:$PORT --workers 1 --threads 8 --timeout 60'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}